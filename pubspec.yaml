name: demo_todo_app
description: "demo Todo App"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0

  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
#  dio: ^5.7.0

  intl: ^0.18.1

  freezed_annotation: ^2.4.4
  shared_preferences: ^2.3.3
  go_router: ^14.6.2

  permission_handler: ^11.3.1 #flutter 공식은 아니지만 flutter에서 권장하는 권한 획득 라이브러리


dev_dependencies:
  flutter_test:
    sdk: flutter


  build_runner: ^2.4.8
  freezed: ^2.5.7
  riverpod_generator: ^2.6.1
  riverpod_lint: ^2.6.1
  flutter_lints: ^5.0.0
  json_serializable: ^6.9.0

flutter:
  uses-material-design: true